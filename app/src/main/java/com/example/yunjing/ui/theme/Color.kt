package com.example.yunjing.ui.theme

import androidx.compose.ui.graphics.Color

// 偏 iOS 的“干净 + 克制”色系：低饱和、柔和对比、强调色清晰
val BrandBlue = Color(0xFF2F6BFF)     // 主强调色（按钮/高亮）
val BrandPurple = Color(0xFF6C5CE7)   // 次强调（用于渐变/点缀）
val SuccessGreen = Color(0xFF22C55E)
val WarningAmber = Color(0xFFF59E0B)
val ErrorRed = Color(0xFFEF4444)

// 背景与中性色（提高“高级感”的关键）
val Surface0 = Color(0xFFF7F8FA)      // App 背景（微灰）
val Surface1 = Color(0xFFFFFFFF)      // 卡片底
val Surface2 = Color(0xFFF1F3F5)      // 次级容器

val TextPrimary = Color(0xFF0F172A)   // 接近 iOS 的深灰黑
val TextSecondary = Color(0xFF475569) // 次级文字
val Divider = Color(0x1A0F172A)       // 10% 透明分割线
